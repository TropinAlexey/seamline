apiVersion: v1
kind: Service
metadata:
  name: seamline-acer-stub
  namespace: seamline
  labels:
    app: seamline-acer-stub
    {{- include "seamline.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: seamline-acer-stub
