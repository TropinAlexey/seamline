apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: seamline-observability
  labels:
    app: prometheus
    {{- include "seamline.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: 9090
  selector:
    app: prometheus
